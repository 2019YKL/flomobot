version: '3'

services:
  flomobot:
    build: .
    container_name: flomobot
    restart: always
    volumes:
      # 挂载配置文件，这样可以在不重建容器的情况下修改配置
      - ./config.py:/app/config.py
    # 设置时区为亚洲/上海
    environment:
      - TZ=Asia/Shanghai
